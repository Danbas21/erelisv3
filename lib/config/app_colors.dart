import 'package:flutter/material.dart';

class AppColors {
  // Colores primarios
  static const Color primaryDarkBlue = Color(0xFF101D36);
  static const Color primaryGreen = Color(0xFF22C55E);
  static const Color primaryOrange = Color(0xFFF59E0B);
  static const Color primaryLightBlue = Color(0xFF3B82F6);

  // Colores secundarios
  static const Color secondaryBlue = Color(0xFF1E3A8A);
  static const Color secondaryTeal = Color(0xFF0D9488);
  static const Color secondaryYellow = Color(0xFFFFD700);

  // Colores de fondo y texto
  static const Color background = Color(0xFF101D36);
  static const Color cardBackground = Color(0xFF172A52);
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textPrimaryNote = Color.fromARGB(255, 20, 4, 112);
  static const Color textSecondary = Color(0xFFB3C5EF);
  static const Color divider = Color(0xFF2A3F6A);

  // Colores de estado
  static const Color success = Color(0xFF22C55E);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6);
  static const Color primary = Color(0xFF3F51B5);
  static const Color primaryDark = Color(0xFF303F9F);
  static const Color primaryLight = Color(0xFFC5CAE9);

  // Colores secundarios
  static const Color accent = Color(0xFFFF4081);
  static const Color accentLight = Color(0xFFFF80AB);

  static const Color card = Colors.white;

  // Colores de texto
  static const Color textHint = Color(0xFFBDBDBD);

  // Colores de estado

  // Colores para subrayado
  static const Color highlightOrange = Color(0xFFFFAB40);
  static const Color highlightGreen = Color(0xFF69F0AE);
  static const Color highlightYellow = Color(0xFFFFFF00);
  // Colores específicos para el tablero
  static const Color pendingColor = Color(0xFFFFF7ED); // Fondo amarillo claro
  static const Color pendingTextColor = Color(0xFFF59E0B); // Texto naranja
  static const Color completedColor = Color(0xFFF0FDF4); // Fondo verde claro
  static const Color completedTextColor = Color(0xFF22C55E); // Texto verde
}
